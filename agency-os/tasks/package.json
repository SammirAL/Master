{
  "name": "@agency-os/tasks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Moteur de tâches d'Agency AI OS : machine à états, dépendances, files, SLA",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agency-os/shared": "workspace:*",
    "@agency-os/database": "workspace:*",
    "bullmq": "^5.34.3",
    "drizzle-orm": "^0.38.3",
    "ioredis": "^5.4.2"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
